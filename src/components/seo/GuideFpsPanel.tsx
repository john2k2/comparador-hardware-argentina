export function GuideFpsPanel() {
  return (
    <p className="text-[11px] md:text-[12px] leading-relaxed normal-case text-foreground/85 font-mono">
      No publicamos FPS estimados para un armado variable. Consultá benchmarks del SKU exacto y verificá que CPU y GPU
      sigan en stock antes de decidir.
    </p>
  );
}
